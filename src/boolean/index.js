function test(num) {  
  // 创建一个掩码，其中从第 1 位到第 15 位都是 1  
  // 0b1111111111111110 是二进制表示，其中第 0 位是 0，第 1 到第 15 位都是 1  
  const mask = 0b1111111111111110;  
    
  // 检查 num 的第 1 到第 15 位是否全部为 0  
  // 使用按位与操作，并检查结果是否为 0  
  return (num & mask) === 0;  
}  
